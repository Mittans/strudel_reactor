import React from 'react';
import InstrumentToggle from './InstrumentToggle';

export default function PreprocControls() {
    return React.createElement(
        React.Fragment,
        null,
        React.createElement(InstrumentToggle, null),
    );
}
